;做过了没必要做Start
; Q1
(define (sign x)
  'YOUR-CODE-HERE
)

; Q2
(define (square x) (* x x))

(define (pow b n)
  'YOUR-CODE-HERE
)
;做过了没必要做End
; Q3
(define (cddr s)
  (cdr (cdr s)))

(define (cadr s)
  (car (cdr s))
)

(define (caddr s)
  'YOUR-CODE-HERE
)

; Q4
(define (ordered? s)
  'YOUR-CODE-HERE
)

; Q5
(define (nodots s)
  'YOUR-CODE-HERE

)

; Q6
(define (empty? s) (null? s))

(define (add s v)
    'YOUR-CODE-HERE
    )

; Q7
; Sets as sorted lists
(define (contains? s v)
    'YOUR-CODE-HERE
    )

; Q8
(define (intersect s t)
    'YOUR-CODE-HERE
    )

(define (union s t)
    'YOUR-CODE-HERE
    )